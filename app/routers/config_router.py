"""Feature flags & config router placeholder for refactor phase."""

from fastapi import APIRouter

router = APIRouter(prefix="/config", tags=["config"])
# TODO: migrar endpoints desde api/feature_flags_api.py y otros componentes de configuración.
